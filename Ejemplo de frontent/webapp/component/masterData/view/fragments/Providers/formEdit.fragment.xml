<c:FragmentDefinition xmlns="sap.m" xmlns:c="sap.ui.core" xmlns:f="sap.ui.layout.form">
    <Dialog
        id="FragmentProvidersEdit"
        title="{i18n>titleProviders}"
        afterClose="onAfterCloseEdit"
        afterOpen="onAfterOpenDialog"
        class="sapUiSizeCompact">
        <HBox id="formContainerEdit">
            <f:SimpleForm id="formProvidersEdit">
                <f:content>
                    <Label text="{i18n>columnNameProviders}" />
                    <Input
                        id="InputNameProviders"
                        placeholder="{i18n>placeHolderNameProviders}"
                        required="true"
                        maxLength="100"
                        change="onValidateField"
                        value="{/mFormEdit/nombre_completo}">
                        <customData>
                            <c:CustomData key="required" value="true" writeToDom="true" />
                        </customData>
                    </Input>
                    <Label text="{i18n>columnAccountNumberProviders}" />
                    <Input
                        id="InputAccountNumberProviders"
                        required="false"
                        maxLength="10"
                        change="onValidateField"
                        placeholder="{i18n>placeHolderAccountNumberProviders}"
                        value="{/mFormEdit/numero_cuenta}">
                        <customData>
                            <c:CustomData key="required" value="true" writeToDom="true" />
                        </customData>
                    </Input>
                    <OverflowToolbar>
                        <ToolbarSpacer />
                        <Button
                            id="btnAddEdit"
                            text="{i18n>btnSave}"
                            type="Accept"
                            press="onEdit"
                            enabled="{/mConfigView/mEditMode}" />
                        <Button
                            id="btnCancelEdit"
                            text="{i18n>btnCancel}"
                            type="Reject"
                            press="onCloseDialog('FragmentProvidersEdit')" />
                    </OverflowToolbar>
                </f:content>
            </f:SimpleForm>
        </HBox>
    </Dialog>
</c:FragmentDefinition>
