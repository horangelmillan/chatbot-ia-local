<c:FragmentDefinition xmlns="sap.m" xmlns:c="sap.ui.core" xmlns:f="sap.ui.layout.form">
    <Dialog
        id="FragmentInternalOrders"
        title="{i18n>titleInternalOrders}"
        afterClose="onAfterClose"
        afterOpen="onAfterOpenDialog"
        class="sapUiSizeCompact">
        <HBox id="formContainerCreate">
            <f:SimpleForm id="FormInternalOrders">
                <f:content>
                    <Label text="{i18n>columnCodInternalOrders}" />
                    <Input
                        id="InputCodInternalOrders"
                        required="true"
                        maxLength="12"
                        change="onValidateField"
                        placeholder="{i18n>placeHolderCodeInternalOrders}"
                        value="{/mFormCreate/codigo}">
                        <customData>
                            <c:CustomData key="required" value="true" writeToDom="true" />
                        </customData>
                    </Input>
                    <Label text="{i18n>columnDescriptionInternalOrders}" />
                    <Input
                        id="InputDescriptionInternalOrders"
                        placeholder="{i18n>placeHolderDescriptionInternalOrders}"
                        required="true"
                        maxLength="50"
                        change="onValidateField"
                        value="{/mFormCreate/descripcion}">
                        <customData>
                            <c:CustomData key="required" value="true" writeToDom="true" />
                        </customData>
                    </Input>
                    <OverflowToolbar>
                        <ToolbarSpacer />
                        <Button
                            id="btnAddCreate"
                            text="{i18n>btnSave}"
                            type="Accept"
                            press="onSave" />
                        <Button
                            id="btnCancelCreate"
                            text="{i18n>btnCancel}"
                            type="Reject"
                            press="onCloseDialog('FragmentInternalOrders')" />
                    </OverflowToolbar>
                </f:content>
            </f:SimpleForm>
        </HBox>
    </Dialog>
</c:FragmentDefinition>
