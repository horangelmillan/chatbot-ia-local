{
  "name": "chatbot-local",
  "private": true,
  "packageManager": "pnpm@11.11.0",
  "scripts": {
    "dev": "concurrently -n BE,FE -c cyan,green \"pnpm run dev:backend\" \"pnpm run dev:frontend\"",
    "dev:backend": "pnpm -C backend start",
    "dev:frontend": "pnpm -C frontend start",
    "test": "concurrently -n BE,FE -c cyan,green \"pnpm run test:backend\" \"pnpm run test:frontend\"",
    "test:all": "concurrently -n BE,FE,E2E -c cyan,green,blue \"pnpm run test:backend\" \"pnpm run test:frontend\" \"pnpm run test:e2e\"",
    "test:coverage": "concurrently -n BE,FE -c cyan,green \"cd backend && pnpm vitest run --coverage\" \"cd frontend && pnpm test\"",
    "test:backend": "cd backend && pnpm vitest run",
    "test:frontend": "cd frontend && pnpm test",
    "test:unit": "concurrently -n BE,FE -c cyan,green \"pnpm run test:unit:backend\" \"pnpm run test:frontend\"",
    "test:unit:backend": "cd backend && pnpm vitest run --exclude '**/postgres/**'",
    "test:integration": "cd backend && pnpm vitest run src/features/documents/adapters/outbound/postgres",
    "test:backend:coverage": "cd backend && pnpm vitest run --coverage",
    "install:all": "cd backend && pnpm install && cd ../frontend && pnpm install && cd ../e2e && pnpm install",
    "test:e2e": "cd e2e && pnpm playwright test",
    "lint": "eslint ."
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "eslint": "^9.21.0",
    "globals": "^15.15.0"
  }
}
